export default function PlayersPage() {
  return (
    <section>
      <p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-700">
        Players
      </p>
      <h2 className="mt-2 text-2xl font-semibold tracking-tight">Player Profiles</h2>
      <p className="mt-2 text-sm text-zinc-600">
        Upcoming slice: profile overview with structured fields, notes, awards,
        and season-specific context.
      </p>
    </section>
  );
}
