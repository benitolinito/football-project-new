export default function ScenariosPage() {
  return (
    <section>
      <p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-700">
        Scenarios
      </p>
      <h2 className="mt-2 text-2xl font-semibold tracking-tight">Sandbox Planning</h2>
      <p className="mt-2 text-sm text-zinc-600">
        Upcoming slice: scenario creation from season snapshots with isolated
        roster and target edits that never mutate official records.
      </p>
    </section>
  );
}
